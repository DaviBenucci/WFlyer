---
schema_version: 2
generated_by: GEMINI_FLASH
generated_at: null
based_on_head: null
context_status: UNINITIALIZED
active_task: null
intended_consumer: null  # CODEX | SONNET
---

# Product Executor Context Pack (`CODEX_CONTEXT.md`)

> Derived, compact context for the next Codex or Sonnet product-executor run. This file is not a specification or architecture authority.
> If `based_on_head` does not match the live state relevant to the task, treat this as historical orientation and verify
> current facts before acting.

## 1. Environment readiness

**Status:** `UNINITIALIZED`

Relevant engineering-tool state only:

- Graphify: `UNINITIALIZED`
- OpenSpec tooling: `UNINITIALIZED`
- Ponytail: `INSTALLED`
- Serena: `UNINITIALIZED`
- Storybook: `UNINITIALIZED`
- Context7/MCP: `UNINITIALIZED`
- Other detected tooling: `UNINITIALIZED`

Do not turn this section into a complete dependency inventory. Link to `TOOLCHAIN_STATE.yaml` for detail.

## 2. Current handoff summary

- From: `UNINITIALIZED`
- Reason: `UNINITIALIZED`
- First next action: `UNINITIALIZED`
- Handoff: `docs/.ai/CURRENT_AGENT_HANDOFF.md`

## 3. Active semantic authority

List only the active/necessary canonical paths and IDs.

- OpenSpec change/spec: `UNINITIALIZED`
- Architecture/governance: `UNINITIALIZED`
- Relevant decision/requirement IDs: `UNINITIALIZED`

Do not copy full semantic specifications here.

## 4. Relevant architecture / graph

Provide a compact, task-specific view only.

```text
UNINITIALIZED
```

Prefer relevant Graphify nodes/edges and exact graph/report paths over a pasted full graph report.

## 5. Relevant code / symbols

- `UNINITIALIZED`

Prefer exact files, classes, functions, symbols and known call paths. Use Serena/Graphify or direct repository inspection
for deeper detail only when the task needs it.

## 6. Relevant tests / validation

- `UNINITIALIZED`

Include the smallest useful test/validation entry points plus any known required final matrix.

## 7. Known findings / constraints

- `UNINITIALIZED`

Distinguish verified facts from hypotheses. Do not copy long historical investigation logs; reference durable evidence.

## 8. Recommended first reads

1. `UNINITIALIZED`

## 9. Recommended first action

`UNINITIALIZED`

## 10. Context-pack discipline

Gemini Flash should regenerate this file when a new Codex or Sonnet turn would materially benefit from an updated compact context.

Keep it concise:

- paths and IDs over duplicated prose;
- relevant nodes/symbols over whole-repository dumps;
- current findings over chat history;
- canonical references over copied specifications;
- exact first action over generic exploration instructions.

Codex or Sonnet may inspect anything necessary for the authorized task; this context pack is an accelerator, not a
boundary on legitimate investigation. However, when a READY handoff records verified facts and `DO_NOT_REDISCOVER`
items, the receiving partner should not repeat broad discovery without evidence that the handoff is stale or inconsistent.
