# Phase 8 Application Branch Contract

Closeout date: 2026-08-28

The isolated Motion Lab implements six Application scenes in canonical story
order:

1. `application-overview` — three concept statements from typed public content;
2. `application-how-it-works` — the five approved steps;
3. `application-benefits` — four approved summary groups;
4. `application-demo` — APP-04 structural demonstration;
5. `application-access` — the sole primary Application conversion action;
6. `application-terminal` — final barline followed by branch terminal.

All six remain semantic vertical content in compact, touch, reduced-motion,
and driver-failure modes. Eligible wide/full-motion viewports use the accepted
Phase-5 native-scroll projection and Phase-6 traversal/history authority.
Phase 8 adds scenes to that architecture; it does not introduce another scroll,
motion, history, or score-position source.

The retained detailed routes `/aplicacao-wflyer`,
`/aplicacao-wflyer/como-funciona`, and `/aplicacao-wflyer/beneficios` remain
independent public documents. Public `/` remains the legacy rollback baseline.
