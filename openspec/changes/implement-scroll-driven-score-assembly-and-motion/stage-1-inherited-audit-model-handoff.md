# Stage-1 Astra High audit-only handoff

**SUPERSEDED_SCOPE (2026-09-22):** ADR-053 / ASM-IMP-DEC-017 removes the
institutional Application branch. This former-topology audit was interrupted
with 500 pending manifest slots and must not be resumed. Preserve its raw
evidence; use `stage-1-inherited-audit-supersession.json` for the current status.
The rest of this file records the earlier authorized handoff as history.

Normalization checkpoint: **PASS — GOVERNANCE SCOPE COMPLETE**. The companion
`stage-1-inherited-audit-governance-validation.json` records the completed strict,
structured, ID/reference, diff and historical integrity checks. This does not
claim runtime geometry, audit completion or Human Geometry Approval.

```text
ROUTING_STATUS=DOWNGRADE_READY
ULTRA_SCOPE_COMPLETE=true
CURRENT_SCOPE_COMPLETE=true
SAFE_TO_DOWNGRADE=true
REMAINING_TASK_CLASS=COMPLEX_DIAGNOSIS
NEXT_MODEL=GPT-6-ASTRA
NEXT_REASONING=HIGH
MODEL_CHANGE_REQUIRED=true
HUMAN_AUTHORIZATION_REQUIRED=false
SAFE_TO_CONTINUE_IMPLEMENTATION=false
```

## Exact repository state

- Repository: `/home/davi-benucci/Área de trabalho/WFlyer`.
- Branch: `develop/site-institucional`.
- HEAD: `40e6ae1a8996c53ed2ec372c47f16bc073d6cee9` (unchanged).
- Active change: `implement-scroll-driven-score-assembly-and-motion`.
- Progress: **7/92**; Human Geometry Approval **PENDING / BLOCKING**.
- Existing dirty runtime/tests/dependencies/other work are preserved. Only
  authorized governance/OpenSpec and derived handoff documents changed during
  normalization. Exact before/after `git status --short` and changed-file list:
  `stage-1-inherited-audit-governance-validation.json`.
- Audit input: 559 current source/asset/test/script/configuration files pinned in
  `stage-1-inherited-audit-source-pins.json`; these integrity paths do not expand
  the audit coverage. Historical source: `306ccb74da6c7bbf8f187e360c0776c571b5fc3d`.
- Approved audit manifest: `stage-1-inherited-audit-manifest.json`.
  SHA-256: `78c04b22d05831a8581b06723bfa12abbde8132ea104f477097024ebf68883a4`.
- Preserved proposal/approval: `stage-1-inherited-audit-owner-decision-proposal.md`,
  `stage-1-inherited-audit-scope.proposed.json`, and verbatim
  `stage-1-inherited-audit-owner-approval.txt`.

## Minimal authority and completed diagnosis

Read AGENTS and its effective `docs/.ai/AI_MODEL_ROUTING_POLICY_v1.1.0.md`, then
CURRENT_HANDOFF and this document. Read only ADR-051/052, ASM-IMP-DEC-015/016,
the canonical `07-successor-inherited-defect-audit-policy.md` (ASM-AUDIT-001),
`known-inherited-findings.json` and its schema, and the active Stage-1 task range.
Older decisions remain binding through the preserved boundary, not as a command
to resume the earlier serial repair workflow. No new architecture choice remains
before the bounded audit; numerical capacity/reservation feasibility is audit work.

ASM-CR-001 is the distinct inherited Access content-reservation root, separate
from ASM-PC-001/002/003. Six saved conditions/four profiles are registered; reuse
`stage-1-access-overflow-classification.md` and its raw data for exact matching
cases. Same-input overflow 62/62 without key, 110/110 verifying, 118/118 failed
script; Soft compact Y325/X5 and Flowing compact Y263/X0 are retained. X5 root
grouping is still explicit NEEDS_REVIEW. Do not redo the completed 48px causal
classification. Historical replay uses current toolchain/browser with the
recorded bundler limitation, not a historical engine recreation/aggregate PASS.

Access scope is approved: scene-owned deterministic complete PRELAUNCH reserve
using existing geometry transport, plus minimum demonstrated local entry/exit
support. **Execution remains paused pending this audit and one batch owner
/governance disposition.** No runtime, geometry, form, production pipeline or
policy repair is permitted in the audit, even if a fix appears obvious.
118px is not the maximum; preserve all ten states plus acknowledgment-pending,
verification/configuration variants, inline fit, typography and focus/provider
bounds. No form shrink/hidden errors, generic vertical rewrite, unrelated
chapter/Projects/Composer change, viewport workaround or feedback loop.

Correct normalized owner path: `src/components/story-score/StoryScoreLayer.tsx`.
The immutable proposal's `src/components/story/StoryScoreLayer.tsx` reference is
a path typo, not a second owner. Other owners are the Application scene/form,
Task-33 ScorePathReview presentation, `organic-flowing.ts` and `projection.ts`.
No source edit is authorized by that ownership list.

## Bounded automated audit

The approved manifest preserves exactly:

- 150 current primary cases (six sources, 50 per engine), with 150 frozen-source
  comparison obligations; no fabricated historical successor-only telemetry.
- 3,240 Access state-observation slots (18 profiles × 15 states/variants × two
  themes × three engines × two source revisions).
- 48 lifecycle and 24 public static-accessibility sequences.
- Existing cases/fixtures in seven score-unit and two component-test sources.

These are a mechanical matrix, not independent reasoning tasks. Generate the
collector/coverage keys from the manifest, batch state transitions, persist raw
structured observations, join equivalent baseline/current automatically, group
occurrences, then deduplicate roots using causal evidence. Keep passing records
on disk; give the model aggregate counts/digests and only differences, failures,
unclassified cases and candidate root clusters. One concise human root report
and occurrence matrices, not thousands of per-slot explanations.

Use one isolated diagnostic workspace/collector. Preserve product/historical
source, original validators, guards/tolerances/readiness and raw failures.
Readiness or first assertion failure must not hide later safe independent
observations. Collector fixes only, with recorded bounded attempts and no third
speculative attempt after two without material progress. No dependency installs,
source repair or automated retries; workers=1/retries=0 for collection.

Before collecting, record exact installed toolchain/browser/font/input metadata
and compare source pins. Match build/bundler/runtime, mode, viewport/DPR, theme,
content/verification, font readiness, seed, interaction and policy inputs in
paired source replay. Compare an explicit common mode separately from actual
policy-selected runtime. Failed/missing input remains UNCLASSIFIED. A transient
VALIDATING state with no committed frame is source coverage, not invented DOM.
44px widget minimum is not a supported visible-interaction maximum. Preserve
unknown provider bounds honestly; control API/script outcomes without real
registration/mail or secrets/tokens in artifacts.

Structured measurements are primary. No per-slot screenshots. Use diagnostic
images only for necessary representative roots, ambiguous classification or
material visual defects, retaining any separately explicit canonical artifact
obligation. This is not the final matrix or the later human-review captures.

Record OUT_OF_AUDIT_SCOPE / NEEDS_REVIEW with available evidence; do not expand
profiles, states, surfaces or launch broad investigation automatically. Stop
unsafe/unauthorized affected actions, continue independent safe declared slots,
and leave missing required coverage visibly blocking. An unavailable case is
not PASS and cannot disappear from the denominator.

## Inventory, invariants and stop

Every observation retains raw outcome, causal class, candidate status/runtime
fallback and stage disposition separately. CURRENT_REGRESSION and inherited
critical/invariant failures block. Individual noncritical DEFER requires later
explicit owner approval with equivalent-not-worse evidence, enabled detection,
accountable repair owner/review and measurable baseline. No auto-DEFER or
must-pass-test reclassification. SAFE_FALLBACK never makes INVALID PASS.

Preserve prior five deltas; 14 Batch-1 repairs/18 occurrences; six Batch-2
repairs/eight occurrences; ASM-PC-001/002/003 and current measurement/read-order/
restoration/candidate/runtime/policy work; ADR-046; Composer seed/fingerprints
`fnv1a32:039bce10` / `fnv1a32:1fe3356b`; approved glyphs; three Projects visits/
NON-ASSEMBLY; 12 physical CSS pixels/full visible staff/event ink; all global
center/visible-segment intersections/tangent/event-safe guards; negative fixtures;
required horizontal qualification and 1100x640 pure geometry checks; all original
validators and prior diagnostics; four historical seals/64 payloads.

Keep existing IDs for known roots. New supported roots may receive the next
unused ASM-AUD-NNN diagnostic ID in the audit inventory under ASM-AUDIT-001;
this does not approve repairs/defer or allocate an ADR. Unclassified clusters
retain collector keys. Propose registry updates for batch governance without
rewriting canonical policy, prior evidence or approved dispositions.

Suggested audit outputs in the active change: `stage-1-inherited-audit-data/`
for structured raw/paired/coverage records and diagnostic tools/provenance;
`stage-1-inherited-audit-inventory.json` for ONE deduplicated batch inventory;
`stage-1-inherited-audit-report.md` for roots/occurrence matrices, limitations and
minimum repair envelopes. Artifact names do not authorize more audit coverage.

When every declared slot has a result or explicit blocker, persist the single
inventory and STOP for one complete-batch owner/governance disposition. Unknown
classification/required coverage remains blocking. Do not begin repairs. Only
a later authorized, ambiguity-free blocking batch normally routes to Sol High.
No Stage 2+, refreeze, new motion, final matrix/captures, commit, push, deploy,
archive or Human Geometry Approval marking.

## Exact first action and ready-to-paste prompt

The first action is to verify manifest/source pins and normalization PASS, then
prepare the isolated **mechanical audit collector and equivalent-input record**
from the approved manifest. Do not begin by reclassifying the known Access delta
or implementing its reservation.

```text
MODEL: GPT-6 ASTRA
REASONING: HIGH
REMAINING_TASK_CLASS=COMPLEX_DIAGNOSIS
TASK: WFlyer Stage-1 systematic inherited-defect audit — AUDIT ONLY.

Resume the exact dirty worktree at
/home/davi-benucci/Área de trabalho/WFlyer
branch develop/site-institucional
expected HEAD 40e6ae1a8996c53ed2ec372c47f16bc073d6cee9.
Active change: implement-scroll-driven-score-assembly-and-motion.
Progress 7/92; Human Geometry Approval PENDING / BLOCKING.

Read AGENTS.md, its approved AI-MRP-001 v1.1.0 reference, CURRENT_HANDOFF.md,
and active-change stage-1-inherited-audit-model-handoff.md. Verify the matching
stage-1-inherited-audit-governance-validation.json PASS, HEAD/status and source
pins. Read only the linked ADR-051/052, ASM-IMP-DEC-015/016, ASM-AUDIT-001 policy,
known-inherited ledger/schema and Stage-1 task range.

FIRST ACTION: verify and consume stage-1-inherited-audit-manifest.json and
stage-1-inherited-audit-source-pins.json; prepare an isolated automated audit
collector and exact paired input/provenance record. Preserve the current dirty
source. Do not restart Stage 1 or repeat the completed 62/110/118 investigation.

Run only the frozen approved scope: 150 current cases plus 150 frozen-source
comparison obligations; 3,240 Access state observations; 48 lifecycle + 24
static-accessibility sequences; existing named deterministic guards. Compare
frozen Phase-9 source 306ccb74da6c7bbf8f187e360c0776c571b5fc3d with exact current
input. Preserve the historical replay limitations, equivalent inputs, raw
assertions and the distinction between common mode and policy-selected mode.

Collect/persist/compare mechanically; group occurrences and deduplicate ROOT
CAUSES. Analyze only differences, failures, unclassified cases and candidate
root clusters. Keep repeated PASS records out of model context. Structured
measurements are primary; no per-observation narratives or screenshots. Use
visual diagnostics only when needed under the approved artifact rules.

Complete every safe independent declared slot despite findings. Keep failed
and unobserved results visible. OUT_OF_AUDIT_SCOPE / NEEDS_REVIEW is recorded
without automatic expansion or broad investigation. No real submissions/mail.

AUDIT ONLY: no product/runtime/geometry/form/pipeline repairs, no validator
weakening, no automatic DEFER, no fallback-as-candidate-PASS, no new reactive
DOM/Projection loop. Even approved ASM-CR-001 scope waits for batch disposition.
Preserve all prior repairs and measurement/capacity work, ADR-046, Composer/
glyphs, three visits/NON-ASSEMBLY, full ink/12px/global zero, all negative
fixtures/validators, old diagnostics and four historical seals/64 payloads.

Produce ONE deduplicated batch inventory with occurrence matrix, lineage and
equivalent-input proof, owner, severity/impact, fallback, recommended disposition,
minimum repair envelope and raw evidence paths. Account for every coverage slot
and explicit blocker. Then STOP for ONE complete-batch owner/governance decision.
Do not begin repairs or route directly to implementation.

No Stage 2+, refreeze, new motion, final browser matrix/final captures, commit,
push, deploy, archive or Human Geometry Approval marking.
```
