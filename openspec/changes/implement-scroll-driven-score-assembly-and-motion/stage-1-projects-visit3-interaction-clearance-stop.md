# Stage-1 Projects visit-3 interaction clearance STOP — 2026-09-12

Status: **BLOCKED_OWNER_DECISION / SCOPE_ESCALATION**. This diagnostic creates
no canonical defect ID or repair authorization. Alias only:
`PROJECTS-VISIT-3-INTERACTION-CLEARANCE`.

HEAD: `40e6ae1a8996c53ed2ec372c47f16bc073d6cee9`, branch
`develop/site-institucional`. Active change:
`implement-scroll-driven-score-assembly-and-motion`, progress 7/92,
Human Geometry Approval pending. Preserve the exact dirty worktree.

## Verified current failure

The independent horizontal candidate now uses the mounted production story,
actual computed production interaction endpoints, a conservative complete
transition envelope, full rendered musical ink and the unchanged 12 physical
CSS pixel requirement.

At 1536x900, the bounded ASM-PC-002/003 work made material progress:

- visit 1 full-event minimum is 12.008125281333946px and visible;
- visit 2 minimum is 29.246151033756192px and visible;
- the complete candidate remains `INSUFFICIENT_CAPACITY`.

The remaining visit-3 global minimum is 8.372450414027085px on the incoming
visit-2-to-3 connector. A separate calculation restricted to staff segments
fully inside visit 3's notation zone (`startT >= 0.88`,
`endT <= 0.9`) is still only 8.475144370079207px. Therefore the complete
interaction envelope cannot pass by changing only the authorized adjacent
junction: visit 3's own shelf also fails the unchanged clearance contract.

The companion JSON records the physical envelope, limiting segment endpoints,
stroke radius, normalized ranges, validation state and attempt history. This
is a current corrected-candidate observation. Historical lineage was not
investigated, and this report does not classify the finding as inherited.

## Exact authorization conflict

- ADR-050 / ASM-IMP-DEC-014 authorizes the second shelf and only the minimum
  adjacent junction geometry on both sides.
- The same decision expressly preserves visit 3's own shelf and requires STOP
  if the repair needs it or exposes another unregistered defect.
- The complete ASM-IMP-DEC-012(D) predicate cannot declare the candidate PASS
  while any visit's production interaction envelope is below 12px.
- Universal fallback cannot conceal failure to qualify an appropriate recorded
  horizontal candidate after the named local repairs.

The owner must decide whether this current visit-3 finding receives a distinct
bounded defect authorization or an explicit amendment to a named repair scope.
No diagnostic alias, model escalation, candidate fallback or implementation
inference grants that authority.

## Preserved implementation state

The valid partial implementation remains in place:

- production interaction-envelope measurement and inert candidate ownership;
- pure complete-capacity evaluation with constraint telemetry;
- generation/readiness/cleanup and whole-story eligibility integration;
- focused capacity and lifecycle tests;
- the current visit-1 cap release that produces 12.008px at 1536x900;
- the already repaired visit-2 result above 29px;
- all Batch-1/Batch-2 repairs, ADR-046 determinism, Composer fingerprints,
  approved glyphs, three Projects visits, NON-ASSEMBLY and global validators.

A rejected smooth arrival-offset variation was reverted after it triggered the
global visible-segment intersection validator. No visit-3 repair remains in the
worktree. The final browser matrix and final captures were not run. No task was
checked, and Human Geometry Approval remains pending.

## Required next step

Per AI-MRP-001 and the owner's routing instruction, continue only with
GPT-6 Astra / High for diagnosis and a concrete minimum owner proposal. Read
this STOP, its companion JSON and only the ADR-048/049/050 and
ASM-IMP-DEC-012/013/014 sections necessary to reconcile the finding.

Do not restart inheritance work, implement visit 3, weaken the complete
predicate, alter cards/fan/focus behavior, redesign Projects, run the final
matrix/captures, start Stage 2+, refreeze, commit, push, deploy or mark Human
Geometry Approval. If the owner later approves a bounded decision, normalize
the minimum governance/OpenSpec contracts before returning implementation to
the least costly suitable profile.
