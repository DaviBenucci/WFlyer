# Stage 1 authorization — portfolio-only rebaseline

## Status

`AUTHORIZED_BOUNDED_IMPLEMENTATION`; Human Geometry Approval remains
`PENDING / BLOCKING`.

ADR-053 and `ASM-IMP-DEC-017` supersede the former active topology. This file
records the current authorization and does not alter frozen historical
Stage-1/Phase-9 evidence.

## Authorized current work

- maintain `HOME / ORIGIN → PROFESSIONAL / PORTFOLIO` on desktop;
- maintain `HOME → Professional chapters → global footer` on mobile;
- remove institutional Application-only routes, components, geometry,
  navigation, responsive state, fixtures, tests, and active documentation;
- preserve and validate the surviving five deltas, Batch-1 repairs, Batch-2
  records/repairs applicable to surviving geometry, ADR-046 determinism,
  Projects decisions, Composer fingerprints, approved glyphs, event safety,
  12px clearance, full ink, and zero self-intersections;
- update the active OpenSpec and derived artifacts;
- run focused/full engineering gates and stop after the rebaseline report.

## Historical dispositions

`ASM-AUDIT-001` is `SUPERSEDED_SCOPE`: 3,192 of 3,692 old-manifest
observations reached a terminal collector state and 500 remained pending.
`auditComplete` is false and no repair authority derives from that inventory.

`ASM-CR-001` is `SUPERSEDED_SCOPE_NOT_REPAIRED_NOT_DEFERRED`. Its prior
Application-access evidence remains intact, but its removed scene is outside the
current product and repair scope.

`ASM-SI-019` remains a preserved historical Batch-2 repair record for the
removed Demo→Launch geometry. `ASM-SI-020..024` remain relevant to surviving
Professional geometry. This rebaseline does not reopen any of them.

## Forbidden work

No visual redesign, new portfolio feature, Composer or glyph change, Projects
redesign, new motion behavior, Stage 2+, final browser matrix, final geometry
captures, Human Geometry Approval, commit, push, deploy, or production change.
No frozen evidence mutation, old-audit restart, or independent application
product deletion.

## Validation and stop

Pass TypeScript, lint, unit/component, production build, focused browser,
strict OpenSpec, structured-document, integrity, derived-artifact, and diff
checks. Fix only failures causally introduced by the rebaseline. If the same
root survives two bounded evidence-based attempts without material progress,
record a handoff and stop. Otherwise stop after the required portfolio-only
implementation report with Human Geometry Approval still pending.
