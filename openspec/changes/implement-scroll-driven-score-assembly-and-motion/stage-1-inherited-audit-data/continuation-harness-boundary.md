# Continuation harness boundary

The owner continuation permits at most one bounded correction per new harness problem, then infrastructure-blocked classification for the affected subset. Product failures do not stop independent observations.

The resumed sequence process (PID 1153305) made no persisted progress for more than eight minutes after skipping all previously recorded Chromium and Firefox lifecycle sequences. Its owned Firefox browser was alive; the next pending sequence was historical Firefox public-no-js-wide. No product failure or successful observation is inferred from this hang. Only that audit process and its owned browser were terminated.

One bounded orchestration correction isolates each unrecorded sequence in a child process with a 120-second external watchdog. It retains every existing raw record, makes no source/validator changes, and never reruns recorded sequence keys. A still-blocked engine/JS-mode sequence family is recorded as infrastructure-blocked and is not attempted again. Other families continue. This does not waive missing substeps or qualify ERROR/NOT_OBSERVED as PASS.

Existing audit key observations: 2,808 distinct keys. Existing lifecycle: 32. Existing static: 8. Existing primary and deterministic guard records are reused.
