# Shared-probe continuation

The initial continuation persisted all 24 pure records and 37 of 42 runtime records before its external 600-second command deadline (exit 124). This is an orchestration interruption, not a product test verdict. Five unrecorded WebKit runtime combinations remain; existing records will not be replayed.

One bounded resume correction adds exact engine/source/mode and engine/source/profile skip sets read from existing raw files. It changes no input, profile, state, constructor, validator, assertion or threshold. The remaining five records are attempted once. If this continuation cannot complete, its remaining mapped obligations are infrastructure-blocked; no second correction or silent scope expansion.
