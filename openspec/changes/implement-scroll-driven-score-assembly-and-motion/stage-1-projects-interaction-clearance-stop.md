# Stage-1 Projects visit-2 focus clearance STOP — 2026-09-11

Status: **BLOCKED_OWNER_DECISION / SCOPE_ESCALATION**. This diagnostic creates
no canonical defect ID or repair authorization. Alias only:
`PROJECTS-VISIT-2-FOCUS-CLEARANCE`.

HEAD: `40e6ae1a8996c53ed2ec372c47f16bc073d6cee9`, branch
`develop/site-institucional`. Active change:
`implement-scroll-driven-score-assembly-and-motion`, progress 7/92,
Human Geometry Approval pending. Preserve the exact dirty worktree.

## Verified current failure

The authorized first High action added three explicit ASM-PC-002 first-shelf
post-repair assertions to the existing focused suite. Result: original five
cases PASS; three new cases FAIL at the recorded ~1.03px idle clearance.
No geometry repair was attempted before collecting required interaction bounds.

Focused Chromium DOM measurement of the three registered candidate viewports
then found a separate second-visit constraint. On the first animation frame
following actual keyboard-visible link focus, Projects card 2 still has its
idle identity transform. Its production focus outline is already solid,
4px wide with 6px offset, while its 360ms transform transition has currentTime=0.
The bottom-center focus-outline edge is too close to the existing upper staff.

| Viewport | Actual focus outline bottom (track CSS px) | Sufficient staff-clearance witness (CSS px) |
| --- | ---: | ---: |
| 1440x900 | 755.734375 | 9.134092525156348 |
| 1536x900 | 758.75 | 9.138670081197802 |
| 1920x917 | 771.15625 | 9.142343177774706 |

Each witness is below the required **12 physical CSS pixels**. The companion
JSON retains raw card/link rectangles, focus-visible status, actual transform,
transition time, two rendered SVG staff endpoints, stroke width and actual
SVG-to-track matrix. Fractional uniform SVG scaling is accounted for. The
witness uses the bottom horizontal outline segment near its midpoint, away
from rounded corners; it establishes failure without claiming the global
minimum. All three focused browser pages reported zero page errors.

This is a current-worktree interaction observation, not a claim of inherited
lineage or final settled-focus failure. No historical investigation was repeated.
The probe did not establish valid focus for its initial card-1 sample, so that
sample is excluded from this finding; only verified card-2 samples are used.
The full diagnostic source is persisted in the JSON; no final captures were
created. Historical first-event ink proof and all prior STOP records are intact.

## Exact authorization conflict

- ASM-IMP-DEC-012(D/E) requires complete production interaction envelopes,
  including transitions, and unchanged >=12px clearance. Settled endpoints or
  idle-only measurements cannot grant complete PASS.
- ASM-IMP-DEC-013(B) authorizes only the first horizontal Projects shelf and
  minimum required entry/visit-1-to-2 continuity. It preserves visits 2/3 shelves
  except minimum continuity proven necessary.
- This witness is on visit 2's own notation shelf at the card's bottom midpoint,
  independent of the known first-event ink deficit. Correcting its focus
  clearance is not automatically an indispensable first-shelf continuity edit.
- ASM-IMP-DEC-013(C/D) and the owner prompt require STOP for unregistered
  geometry/scope expansion, and prohibit universal fallback from concealing
  failure to qualify the three horizontal candidates within the approved scope.

No second-shelf change, focus-style change, outline exclusion, reduced clearance,
viewport exception or broader fan repair is authorized. ASM-PC-001 and
ASM-PC-002 remain separate and their existing approvals remain valid. The
new focus response needs an explicit owner decision before implementation can
continue; a model upgrade alone cannot supply that decision.

## Work preserved and checks

This High run changed only the focused capacity test in implementation areas:
three pending post-repair cases were added; the original five were retained.
Runtime geometry, Projects styles, evaluator, original fixtures, Batch 1/2,
ADR-046, Composer/glyphs and all historical evidence are unchanged. No runtime
repair, policy integration, vertical support, final browser matrix or final
capture was performed. The owned port-3100 development server and browser
processes were closed. No checkbox is completed.

The companion JSON records the exact focused test output, current source
hashes, bounded strict/structured/diff and historical checks, and final Git
status. Graphify's required AST-only update follows the test-source edit.
These checks document a STOP, not Stage-1 runtime acceptance.

## Next decision and bounded continuation

The owner must decide how to handle the verified visit-2 focus-clearance
constraint while preserving the complete interaction contract. Any separate
Astra Ultra review is **diagnosis/proposal only** until that response has explicit
approval. First review the saved witness and 012(D/E)/013(B–D); do not restart
the completed ASM-PC-001 or ASM-PC-002 inheritance investigations. Determine
whether a distinct bounded authorization or a named-scope amendment is needed;
do not silently call this first-shelf continuity or implement a horizontal repair.

The partial projects-capacity.ts and its original five tests remain starting
work; the three new ASM-PC-002 assertions intentionally remain failing while
repair is pending. Once the new decision is resolved, normalize only required
contracts and preserve these tests and fixtures before resuming implementation.
No broad audit, final browser matrix/captures, Stage 2+, refreeze, successor
motion, archive, public cutover, commit, push, deploy or self-approved human gate.
